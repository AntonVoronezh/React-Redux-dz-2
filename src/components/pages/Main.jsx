import React from 'react';
import PropTypes from 'prop-types';

const Main = () => {
	return <div>Main</div>;
};

export default Main;

Main.propTypes = {
    name: PropTypes.string
  };

